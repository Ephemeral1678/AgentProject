# PyTorch (根据CUDA版本选择, 推荐2.0+)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0
tqdm>=4.65.0
transformers>=4.35.0

# 注意: transformers 仅用于CLIP文本编码器(ViT-B/32)
# 如CLIP加载失败, 会自动降级为内置的SimpleTextEncoder(BiLSTM), 无需额外依赖
